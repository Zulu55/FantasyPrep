@if (matchDTO is null)
{
    <Loading />
}
else
{
    <MudDialog>
        <DialogContent>
            <AddMatchForm @ref="addMatchForm" MatchDTO="matchDTO" OnValidSubmit="AddAsync" ReturnAction="Return" />
        </DialogContent>
    </MudDialog>
}